# QWERTY bindings
bind f:${ac-slater-key-modifier} ${resize-full}

bind h:${ac-slater-key-modifier} ${1-2-L}
bind ;:${ac-slater-key-modifier} ${1-2-R}

bind y:${ac-slater-key-modifier} ${1-2-TL}
bind p:${ac-slater-key-modifier} ${1-2-TR}
bind n:${ac-slater-key-modifier} ${1-2-BL}
bind /:${ac-slater-key-modifier} ${1-2-BR}

bind h:${ac-slater-key-modifier-super} ${2-3-L}
bind ;:${ac-slater-key-modifier-super} ${2-3-R}

bind y:${ac-slater-key-modifier-super} ${2-3-TL}
bind p:${ac-slater-key-modifier-super} ${2-3-TR}
bind n:${ac-slater-key-modifier-super} ${2-3-BL}
bind /:${ac-slater-key-modifier-super} ${2-3-BR}

bind u:${ac-slater-key-modifier} ${1-3-TL}
bind i:${ac-slater-key-modifier} ${1-3-TM}
bind o:${ac-slater-key-modifier} ${1-3-TR}

bind j:${ac-slater-key-modifier} ${1-3-L}
bind k:${ac-slater-key-modifier} ${1-3-M}
bind l:${ac-slater-key-modifier} ${1-3-R}

bind m:${ac-slater-key-modifier} ${1-3-BL}
bind ,:${ac-slater-key-modifier} ${1-3-BM}
bind .:${ac-slater-key-modifier} ${1-3-BR}
